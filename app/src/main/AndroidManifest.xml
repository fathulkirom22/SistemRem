<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bahwell.sistemrem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".petunjuk"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".materi"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".evaluasi"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".tentang"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".tampilkan_materi"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".score"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|screenSize"/>
    </application>

</manifest>